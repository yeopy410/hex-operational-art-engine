"use strict";

export const SCALE_DELTA_MUL = 0.00075,
  SCALE_MIN = 0.1, SCALE_MAX = 1,
  // GRID_RATIO = [3**0.5/2, 1/2],
  GRID_SIZE = [70, 40], // [40*2*Math.sqrt(3) /2, 160 /4].map(Math.round)
  HEX_BORDER = 0.5,
  UNIT_SIZE = [100, 100],
  UNIT_BORDER = 2;