"use strict";

export const SCALE_DELTA_MUL = 0.00075,
  SCALE_MIN = 0.15, SCALE_MAX = 1,
  // GRID_RATIO = [3**0.5/2, 1/2],
  GRID_SIZE = [43, 25], // [25*2*Math.sqrt(3) /2, 100 /4].map(Math.round)
  PADDING = 2,
  UNIT_SIZE = [60, 60];